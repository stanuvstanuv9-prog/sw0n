local success, launchError = pcall(function()
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local UserInputService = game:GetService("UserInputService")
    local CoreGui = game:GetService("CoreGui")
    local TweenService = game:GetService("TweenService")
    local Lighting = game:GetService("Lighting")
    local VirtualUser = game:GetService("VirtualUser")
    local Workspace = game:GetService("Workspace")

    local LocalPlayer = Players.LocalPlayer
    if not LocalPlayer then
        Players:GetPropertyChangedSignal("LocalPlayer"):Wait()
        LocalPlayer = Players.LocalPlayer
    end

    local PlayerGui = LocalPlayer:WaitForChild("PlayerGui", 10)

    local Config = {
        CFrameSpeed = false,
        SpeedValue = 2.5,
        NoFall = false,
        Noclip = false,
        AntiAFK = false,
        AutoSprint = false,
        NormalWalkSpeed = 16,
        SprintWalkSpeed = 25,
        ESP = false,
        Box = false,
        Highlight = false,
        Tracers = false,
        Fullbright = false,
        FOVValue = 70,
        WallColor = false,
        WallColorValue = Color3.fromRGB(120, 100, 255),
        SkyColor = false,
        SkyColorValue = Color3.fromRGB(0, 180, 255),
        Particles = false,
        ParticleMode = "Snow",
        PerfectSkillCheck = false
    }

    local connections = {}
    local tabs = {}
    local tabButtons = {}
    local isSprinting = false

    local originalLighting = {
        Ambient = Lighting.Ambient,
        OutdoorAmbient = Lighting.OutdoorAmbient,
        Brightness = Lighting.Brightness,
        ClockTime = Lighting.ClockTime,
        FogEnd = Lighting.FogEnd,
        FogColor = Lighting.FogColor,
        GlobalShadows = Lighting.GlobalShadows,
        ColorShift_Top = Lighting.ColorShift_Top
    }

    local Camera = Workspace.CurrentCamera or Workspace:FindFirstChildOfClass("Camera")
    Workspace:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
        Camera = Workspace.CurrentCamera
    end)

    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "Luna_V4"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    local coreOk = pcall(function() ScreenGui.Parent = CoreGui end)
    if not coreOk or not ScreenGui.Parent then
        if PlayerGui then ScreenGui.Parent = PlayerGui else return end
    end

    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 460, 0, 300)
    MainFrame.Position = UDim2.new(0.5, -230, 0.5, -150)
    MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    MainFrame.BorderSizePixel = 0
    MainFrame.ClipsDescendants = true
    MainFrame.Parent = ScreenGui

    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 8)
    UICorner.Parent = MainFrame

    do
        local dragging, dragStart, startPos
        MainFrame.InputBegan:Connect(function(inp)
            if inp.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = true
                dragStart = inp.Position
                startPos = MainFrame.Position
            end
        end)
        MainFrame.InputEnded:Connect(function(inp)
            if inp.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(inp)
            if dragging and inp.UserInputType == Enum.UserInputType.MouseMovement then
                local delta = inp.Position - dragStart
                MainFrame.Position = UDim2.new(
                    startPos.X.Scale, startPos.X.Offset + delta.X,
                    startPos.Y.Scale, startPos.Y.Offset + delta.Y
                )
            end
        end)
    end

    local Sidebar = Instance.new("Frame")
    Sidebar.Size = UDim2.new(0, 120, 1, 0)
    Sidebar.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
    Sidebar.BorderSizePixel = 0
    Sidebar.Parent = MainFrame

    local SidebarCorner = Instance.new("UICorner")
    SidebarCorner.CornerRadius = UDim.new(0, 8)
    SidebarCorner.Parent = Sidebar

    local Logo = Instance.new("TextLabel")
    Logo.Size = UDim2.new(1, 0, 0, 40)
    Logo.BackgroundTransparency = 1
    Logo.Text = "LUNA V4"
    Logo.TextColor3 = Color3.fromRGB(0, 180, 255)
    Logo.Font = Enum.Font.GothamBold
    Logo.TextSize = 16
    Logo.Parent = Sidebar

    local TabBtnContainer = Instance.new("Frame")
    TabBtnContainer.Size = UDim2.new(1, 0, 1, -50)
    TabBtnContainer.Position = UDim2.new(0, 0, 0, 50)
    TabBtnContainer.BackgroundTransparency = 1
    TabBtnContainer.Parent = Sidebar

    local TabListLayout = Instance.new("UIListLayout")
    TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabListLayout.Padding = UDim.new(0, 4)
    TabListLayout.Parent = TabBtnContainer

    local ContentFrame = Instance.new("Frame")
    ContentFrame.Size = UDim2.new(1, -130, 1, -10)
    ContentFrame.Position = UDim2.new(0, 125, 0, 5)
    ContentFrame.BackgroundTransparency = 1
    ContentFrame.Parent = MainFrame

    local function SwitchToTab(tabName)
        for name, frame in pairs(tabs) do
            frame.Visible = (name == tabName)
        end
        for name, btn in pairs(tabButtons) do
            btn.TextColor3 = (name == tabName)
                and Color3.fromRGB(0, 180, 255)
                or Color3.fromRGB(150, 150, 150)
        end
    end

    local function CreateTab(name)
        local TabButton = Instance.new("TextButton")
        TabButton.Size = UDim2.new(1, 0, 0, 32)
        TabButton.BackgroundTransparency = 1
        TabButton.Text = "  " .. name
        TabButton.TextColor3 = Color3.fromRGB(150, 150, 150)
        TabButton.Font = Enum.Font.GothamMedium
        TabButton.TextSize = 13
        TabButton.TextXAlignment = Enum.TextXAlignment.Left
        TabButton.Parent = TabBtnContainer
        tabButtons[name] = TabButton

        local Page = Instance.new("ScrollingFrame")
        Page.Size = UDim2.new(1, 0, 1, 0)
        Page.BackgroundTransparency = 1
        Page.Visible = false
        Page.CanvasSize = UDim2.new(0, 0, 0, 0)
        Page.AutomaticCanvasSize = Enum.AutomaticSize.Y
        Page.ScrollBarThickness = 3
        Page.ScrollBarImageColor3 = Color3.fromRGB(0, 180, 255)
        Page.Parent = ContentFrame
        tabs[name] = Page

        local PageLayout = Instance.new("UIListLayout")
        PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
        PageLayout.Padding = UDim.new(0, 6)
        PageLayout.Parent = Page

        local PagePadding = Instance.new("UIPadding")
        PagePadding.PaddingTop = UDim.new(0, 4)
        PagePadding.Parent = Page

        TabButton.MouseButton1Click:Connect(function()
            SwitchToTab(name)
        end)

        return Page
    end

    local MainTab = CreateTab("Main")
    local VisualsTab = CreateTab("Visuals")
    local ExploitsTab = CreateTab("Exploits")
    SwitchToTab("Main")

    local function CreateToggle(parent, labelText, propName, callback, hasColor, colorPropName, colorCallback)
        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(1, -5, 0, 35)
        Frame.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
        Frame.BorderSizePixel = 0
        Frame.Parent = parent

        local FCorn = Instance.new("UICorner")
        FCorn.CornerRadius = UDim.new(0, 4)
        FCorn.Parent = Frame

        local Label = Instance.new("TextLabel")
        Label.Size = UDim2.new(1, -90, 1, 0)
        Label.Position = UDim2.new(0, 10, 0, 0)
        Label.BackgroundTransparency = 1
        Label.Text = labelText
        Label.TextColor3 = Color3.fromRGB(220, 220, 220)
        Label.Font = Enum.Font.GothamMedium
        Label.TextSize = 12
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.Parent = Frame

        local Btn = Instance.new("TextButton")
        Btn.Size = UDim2.new(0, 35, 0, 16)
        Btn.Position = UDim2.new(1, -45, 0.5, -8)
        Btn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        Btn.Text = ""
        Btn.Parent = Frame

        local BCorn = Instance.new("UICorner")
        BCorn.CornerRadius = UDim.new(1, 0)
        BCorn.Parent = Btn

        Btn.MouseButton1Click:Connect(function()
            Config[propName] = not Config[propName]
            TweenService:Create(Btn, TweenInfo.new(0.15), {
                BackgroundColor3 = Config[propName]
                    and Color3.fromRGB(0, 180, 255)
                    or Color3.fromRGB(45, 45, 55)
            }):Play()
            if callback then callback(Config[propName]) end
        end)

        if hasColor and colorPropName then
            local ColorBtn = Instance.new("TextButton")
            ColorBtn.Size = UDim2.new(0, 16, 0, 16)
            ColorBtn.Position = UDim2.new(1, -75, 0.5, -8)
            ColorBtn.BackgroundColor3 = Config[colorPropName]
            ColorBtn.Text = ""
            ColorBtn.Parent = Frame

            local CCorn = Instance.new("UICorner")
            CCorn.CornerRadius = UDim.new(0, 4)
            CCorn.Parent = ColorBtn

            local Palette = Instance.new("Frame")
            Palette.Size = UDim2.new(0, 100, 0, 56)
            Palette.Position = UDim2.new(1, -110, 1, 2)
            Palette.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
            Palette.BorderSizePixel = 0
            Palette.ZIndex = 10
            Palette.Visible = false
            Palette.Parent = Frame

            local PCorn = Instance.new("UICorner")
            PCorn.CornerRadius = UDim.new(0, 4)
            PCorn.Parent = Palette

            local PGrid = Instance.new("UIGridLayout")
            PGrid.CellSize = UDim2.new(0, 20, 0, 20)
            PGrid.CellPadding = UDim2.new(0, 4, 0, 4)
            PGrid.SortOrder = Enum.SortOrder.LayoutOrder
            PGrid.Parent = Palette

            local PPad = Instance.new("UIPadding")
            PPad.PaddingLeft = UDim.new(0, 4)
            PPad.PaddingTop = UDim.new(0, 4)
            PPad.Parent = Palette

            local colors = {
                Color3.fromRGB(255, 0, 0), Color3.fromRGB(0, 255, 0),
                Color3.fromRGB(0, 0, 255), Color3.fromRGB(255, 255, 0),
                Color3.fromRGB(255, 0, 255), Color3.fromRGB(0, 255, 255),
                Color3.fromRGB(255, 255, 255), Color3.fromRGB(120, 100, 255)
            }
            for _, col in ipairs(colors) do
                local Choice = Instance.new("TextButton")
                Choice.Size = UDim2.new(0, 20, 0, 20)
                Choice.BackgroundColor3 = col
                Choice.Text = ""
                Choice.ZIndex = 11
                Choice.Parent = Palette

                local ChoiceCorn = Instance.new("UICorner")
                ChoiceCorn.CornerRadius = UDim.new(0, 3)
                ChoiceCorn.Parent = Choice

                Choice.MouseButton1Click:Connect(function()
                    Config[colorPropName] = col
                    ColorBtn.BackgroundColor3 = col
                    Palette.Visible = false
                    if colorCallback then colorCallback(col) end
                end)
            end

            ColorBtn.MouseButton1Click:Connect(function()
                Palette.Visible = not Palette.Visible
            end)
        end
    end

    table.insert(connections, RunService.RenderStepped:Connect(function(dt)
        if not LocalPlayer.Character then return end
        local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hrp and hum and hum.MoveDirection.Magnitude > 0 and Config.CFrameSpeed then
            local spd = Config.SpeedValue
            if Config.AutoSprint and isSprinting then spd = spd * 1.8 end
            hrp.CFrame = hrp.CFrame + (hum.MoveDirection * (spd * dt * 10))
        end
    end))

    table.insert(connections, RunService.RenderStepped:Connect(function()
        if not LocalPlayer.Character then return end
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if not hum then return end
        if Config.NoFall and hum:GetState() == Enum.HumanoidStateType.Freefall then
            hum:ChangeState(Enum.HumanoidStateType.Running)
        end
        if not Config.CFrameSpeed and Config.AutoSprint then
            if isSprinting then
                hum.WalkSpeed = Config.SprintWalkSpeed
            else
                if hum.WalkSpeed == Config.SprintWalkSpeed then
                    hum.WalkSpeed = Config.NormalWalkSpeed
                end
            end
        end
    end))

    table.insert(connections, RunService.Stepped:Connect(function()
        if Config.Noclip and LocalPlayer.Character then
            for _, part in ipairs(LocalPlayer.Character:GetChildren()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end))

    table.insert(connections, RunService.RenderStepped:Connect(function()
        if Camera and Camera.FieldOfView ~= Config.FOVValue then
            Camera.FieldOfView = Config.FOVValue
        end
    end))

    table.insert(connections, RunService.RenderStepped:Connect(function()
        if Config.Fullbright then
            Lighting.Ambient = Color3.fromRGB(255, 255, 255)
            Lighting.OutdoorAmbient = Color3.fromRGB(255, 255, 255)
            Lighting.Brightness = 3
            Lighting.ClockTime = 14
            Lighting.FogEnd = 999999
            Lighting.GlobalShadows = false
        end
    end))

    local antiAfkConnection
    local function toggleAntiAFK(state)
        if state then
            if not antiAfkConnection then
                antiAfkConnection = LocalPlayer.Idled:Connect(function()
                    pcall(function()
                        VirtualUser:CaptureController()
                        VirtualUser:ClickButton2(Vector2.new(0, 0))
                    end)
                end)
            end
        else
            if antiAfkConnection then
                antiAfkConnection:Disconnect()
                antiAfkConnection = nil
            end
        end
    end

    local function toggleFullbright(state)
        if not state then
            Lighting.Ambient = originalLighting.Ambient
            Lighting.OutdoorAmbient = originalLighting.OutdoorAmbient
            Lighting.Brightness = originalLighting.Brightness
            Lighting.ClockTime = originalLighting.ClockTime
            Lighting.FogEnd = originalLighting.FogEnd
            Lighting.GlobalShadows = originalLighting.GlobalShadows
            Lighting.ColorShift_Top = originalLighting.ColorShift_Top
        end
    end

    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then return end
        if input.KeyCode == Enum.KeyCode.LeftShift or input.KeyCode == Enum.KeyCode.RightShift then
            isSprinting = true
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.KeyCode == Enum.KeyCode.LeftShift or input.KeyCode == Enum.KeyCode.RightShift then
            isSprinting = false
            if LocalPlayer.Character then
                local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                if hum and not Config.CFrameSpeed and hum.WalkSpeed == Config.SprintWalkSpeed then
                    hum.WalkSpeed = Config.NormalWalkSpeed
                end
            end
        end
    end)

    local function applyWallMod(state)
        if state then
            Lighting.Ambient = Config.WallColorValue
        else
            Lighting.Ambient = originalLighting.Ambient
        end
    end

    local function applySkyMod(state)
        if state then
            Lighting.OutdoorAmbient = Config.SkyColorValue
            Lighting.ColorShift_Top = Config.SkyColorValue
            Lighting.FogColor = Config.SkyColorValue
        else
            Lighting.OutdoorAmbient = originalLighting.OutdoorAmbient
            Lighting.ColorShift_Top = originalLighting.ColorShift_Top
            Lighting.FogColor = originalLighting.FogColor
        end
    end

    local function scansForWeapons(container)
        if not container then return false end
        for _, item in ipairs(container:GetChildren()) do
            if item:IsA("Tool") then
                local iName = item.Name:lower()
                if not iName:find("flashlight") and not iName:find("medkit")
                   and not iName:find("cola") and not iName:find("camera") then
                    return true
                end
            end
        end
        return false
    end

    local function checkIsKiller(player, character)
        if not player then return false end
        if player.Team then
            local tName = player.Team.Name:lower()
            if tName:find("kill") or tName:find("slash") or tName:find("beast")
               or tName:find("murder") or tName:find("hunter") then
                return true
            end
        end
        if player:GetAttribute("Role") == "Killer" or player:GetAttribute("IsKiller") == true then
            return true
        end
        if character and (character:GetAttribute("Role") == "Killer"
            or character:GetAttribute("IsKiller") == true) then
            return true
        end
        if character and scansForWeapons(character) then return true end
        if player:FindFirstChild("Backpack") and scansForWeapons(player.Backpack) then return true end
        return false
    end

    local function getOrCreateBillboard(character, color, labelText)
        local folder = character:FindFirstChild("Luna_Visuals_Container")
        if not folder then
            folder = Instance.new("Folder")
            folder.Name = "Luna_Visuals_Container"
            folder.Parent = character
        end

        local hrp = character:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local bb = folder:FindFirstChild("LunaBB")
        if Config.ESP then
            if not bb then
                bb = Instance.new("BillboardGui")
                bb.Name = "LunaBB"
                bb.AlwaysOnTop = true
                bb.Size = UDim2.new(0, 120, 0, 40)
                bb.StudsOffset = Vector3.new(0, 3, 0)
                bb.Adornee = hrp
                bb.Parent = folder

                local tl = Instance.new("TextLabel")
                tl.Name = "NameLabel"
                tl.Size = UDim2.new(1, 0, 1, 0)
                tl.BackgroundTransparency = 1
                tl.Font = Enum.Font.GothamBold
                tl.TextSize = 13
                tl.TextStrokeTransparency = 0.5
                tl.Parent = bb
            end
            local tl = bb:FindFirstChild("NameLabel")
            if tl then
                tl.Text = labelText
                tl.TextColor3 = color
            end
        else
            if bb then bb:Destroy() end
        end

        local sel = folder:FindFirstChild("LunaBox")
        if Config.Box then
            if not sel then
                sel = Instance.new("SelectionBox")
                sel.Name = "LunaBox"
                sel.LineThickness = 0.05
                sel.SurfaceTransparency = 0.7
                sel.Adornee = character
                sel.Parent = folder
            end
            sel.Color3 = color
            sel.SurfaceColor3 = color
        else
            if sel then sel:Destroy() end
        end

        local tracerGui = ScreenGui:FindFirstChild("TracerFrame")
        if Config.Tracers then
            if not tracerGui then
                tracerGui = Instance.new("Frame")
                tracerGui.Name = "TracerFrame"
                tracerGui.Size = UDim2.new(1, 0, 1, 0)
                tracerGui.BackgroundTransparency = 1
                tracerGui.Parent = ScreenGui
            end
        end
    end

    task.spawn(function()
        while task.wait(0.1) do
            for _, player in ipairs(Players:GetPlayers()) do
                if player == LocalPlayer then continue end
                local character = player.Character
                if not character then continue end
                local hrp = character:FindFirstChild("HumanoidRootPart")
                if not hrp then continue end

                local isKiller = checkIsKiller(player, character)
                local espColor = isKiller and Color3.fromRGB(255, 30, 30) or Color3.fromRGB(0, 255, 120)
                local espText = isKiller
                    and ("[ МАНЬЯК ] " .. player.Name)
                    or ("[ Выживший ] " .. player.Name)

                getOrCreateBillboard(character, espColor, espText)

                local highlight = character:FindFirstChild("LunaHighlight")
                if Config.Highlight then
                    if not highlight then
                        highlight = Instance.new("Highlight")
                        highlight.Name = "LunaHighlight"
                        highlight.FillTransparency = 0.6
                        highlight.OutlineTransparency = 0.1
                        highlight.Parent = character
                    end
                    highlight.FillColor = espColor
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                else
                    if highlight then highlight:Destroy() end
                end
            end

            for _, child in ipairs(Workspace:GetChildren()) do
                local folder = child:FindFirstChild("Luna_Visuals_Container")
                if folder then
                    local stillInGame = false
                    for _, pl in ipairs(Players:GetPlayers()) do
                        if pl.Character == child then stillInGame = true break end
                    end
                    if not stillInGame then folder:Destroy() end
                end
            end
        end
    end)

    table.insert(connections, RunService.RenderStepped:Connect(function()
        local tf = ScreenGui:FindFirstChild("TracerFrame")
        if not tf then return end
        for _, c in ipairs(tf:GetChildren()) do c:Destroy() end
        if not Config.Tracers then return end

        local vp = Camera.ViewportSize
        local origin = Vector2.new(vp.X / 2, vp.Y)

        for _, player in ipairs(Players:GetPlayers()) do
            if player == LocalPlayer then continue end
            local character = player.Character
            if not character then continue end
            local hrp = character:FindFirstChild("HumanoidRootPart")
            if not hrp then continue end

            local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
            if not onScreen then continue end

            local isKiller = checkIsKiller(player, character)
            local lineColor = isKiller and Color3.fromRGB(255, 60, 60) or Color3.fromRGB(0, 255, 120)

            local target = Vector2.new(screenPos.X, screenPos.Y)
            local delta = target - origin
            local length = delta.Magnitude
            local angle = math.atan2(delta.Y, delta.X)

            local line = Instance.new("Frame")
            line.BackgroundColor3 = lineColor
            line.BorderSizePixel = 0
            line.AnchorPoint = Vector2.new(0, 0.5)
            line.Position = UDim2.new(0, origin.X, 0, origin.Y)
            line.Size = UDim2.new(0, length, 0, 1)
            line.Rotation = math.deg(angle)
            line.Parent = tf
        end
    end))

    local function UpdateParticles()
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local existing = hrp:FindFirstChild("LunaWeatherAttach")
        if Config.Particles then
            if not existing then
                existing = Instance.new("Attachment", hrp)
                existing.Name = "LunaWeatherAttach"
                existing.Position = Vector3.new(0, 15, 0)
                local pe = Instance.new("ParticleEmitter", existing)
                pe.Name = "WeatherEmitter"
                pe.SpreadAngle = Vector2.new(60, 60)
                pe.EmissionDirection = Enum.NormalId.Bottom
            end
            local pe = existing:FindFirstChild("WeatherEmitter")
            if pe then
                if Config.ParticleMode == "Snow" then
                    pe.Texture = "rbxassetid://281983280"
                    pe.Rate = 400
                    pe.Speed = NumberRange.new(15, 25)
                    pe.Lifetime = NumberRange.new(3, 5)
                    pe.Size = NumberSequence.new({NumberSequenceKeypoint.new(0, 0.4), NumberSequenceKeypoint.new(1, 0.4)})
                    pe.Transparency = NumberSequence.new(0.1)
                    pe.Color = ColorSequence.new(Color3.fromRGB(255, 255, 255))
                else
                    pe.Texture = "rbxassetid://243660364"
                    pe.Rate = 600
                    pe.Speed = NumberRange.new(70, 90)
                    pe.Lifetime = NumberRange.new(0.5, 1)
                    pe.Size = NumberSequence.new({NumberSequenceKeypoint.new(0, 0.1), NumberSequenceKeypoint.new(1, 0.1)})
                    pe.Transparency = NumberSequence.new(0.6)
                    pe.Color = ColorSequence.new(Color3.fromRGB(180, 220, 255))
                end
            end
        else
            if existing then existing:Destroy() end
        end
    end

    task.spawn(function()
        while task.wait(0.5) do
            UpdateParticles()
        end
    end)

    task.spawn(function()
        while task.wait(0.05) do
            if not Config.PerfectSkillCheck then continue end
            local pgui = LocalPlayer:FindFirstChild("PlayerGui")
            if not pgui then continue end
            for _, sg in ipairs(pgui:GetChildren()) do
                local function findSkillCheck(obj)
                    for _, child in ipairs(obj:GetDescendants()) do
                        local n = child.Name:lower()
                        if n:find("skillcheck") or n:find("skill_check") or n:find("greatzone") then
                            if child:IsA("Frame") and child.Visible then
                                local vInput = Instance.new("InputObject")
                                vInput.KeyCode = Enum.KeyCode.Space
                                vInput.UserInputType = Enum.UserInputType.Keyboard
                                UserInputService.InputBegan:Fire(vInput, false)
                            end
                        end
                    end
                end
                findSkillCheck(sg)
            end
        end
    end)

    CreateToggle(MainTab, "Speedhack (CFrame)", "CFrameSpeed")
    CreateToggle(MainTab, "Авто спринт (Shift)", "AutoSprint")
    CreateToggle(MainTab, "Noclip (Сквозь стены)", "Noclip")
    CreateToggle(MainTab, "No Fall (Анти-падение)", "NoFall")
    CreateToggle(MainTab, "Anti-AFK (Защита от кика)", "AntiAFK", toggleAntiAFK)

    CreateToggle(VisualsTab, "ESP (Имена игроков)", "ESP")
    CreateToggle(VisualsTab, "Box ESP (Рамки)", "Box")
    CreateToggle(VisualsTab, "Highlight (Обводка)", "Highlight")
    CreateToggle(VisualsTab, "Tracers (Линии)", "Tracers")
    CreateToggle(VisualsTab, "Fullbright (Яркость)", "Fullbright", toggleFullbright)
    CreateToggle(VisualsTab, "Цвет стен", "WallColor", applyWallMod,
        true, "WallColorValue", function(col)
            Config.WallColorValue = col
            if Config.WallColor then applyWallMod(true) end
        end)
    CreateToggle(VisualsTab, "Цвет неба", "SkyColor", applySkyMod,
        true, "SkyColorValue", function(col)
            Config.SkyColorValue = col
            if Config.SkyColor then applySkyMod(true) end
        end)
    
    CreateToggle(VisualsTab, "Частицы (Погода)", "Particles", function()
        UpdateParticles()
    end)

    do
        local WeatherFrame = Instance.new("Frame")
        WeatherFrame.Size = UDim2.new(1, -5, 0, 35)
        WeatherFrame.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
        WeatherFrame.BorderSizePixel = 0
        WeatherFrame.Parent = VisualsTab

        local WCorn = Instance.new("UICorner")
        WCorn.CornerRadius = UDim.new(0, 4)
        WCorn.Parent = WeatherFrame

        local WLabel = Instance.new("TextLabel")
        WLabel.Size = UDim2.new(0, 160, 1, 0)
        WLabel.Position = UDim2.new(0, 10, 0, 0)
        WLabel.BackgroundTransparency = 1
        WLabel.Text = "Режим погоды"
        WLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
        WLabel.Font = Enum.Font.GothamMedium
        WLabel.TextSize = 12
        WLabel.TextXAlignment = Enum.TextXAlignment.Left
        WLabel.Parent = WeatherFrame

        local WBtn = Instance.new("TextButton")
        WBtn.Size = UDim2.new(0, 75, 0, 20)
        WBtn.Position = UDim2.new(1, -83, 0.5, -10)
        WBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        WBtn.Text = "Снег"
        WBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        WBtn.Font = Enum.Font.GothamBold
        WBtn.TextSize = 11
        WBtn.Parent = WeatherFrame

        local WBCorn = Instance.new("UICorner")
        WBCorn.CornerRadius = UDim.new(0, 4)
        WBCorn.Parent = WBtn

        WBtn.MouseButton1Click:Connect(function()
            if Config.ParticleMode == "Snow" then
                Config.ParticleMode = "Rain"
                WBtn.Text = "Дождь"
            else
                Config.ParticleMode = "Snow"
                WBtn.Text = "Снег"
            end
            if Config.Particles then
                UpdateParticles()
            end
        end)
    end

    do
        local FOVFrame = Instance.new("Frame")
        FOVFrame.Size = UDim2.new(1, -5, 0, 35)
        FOVFrame.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
        FOVFrame.BorderSizePixel = 0
        FOVFrame.Parent = VisualsTab

        local FOVCorn = Instance.new("UICorner")
        FOVCorn.CornerRadius = UDim.new(0, 4)
        FOVCorn.Parent = FOVFrame

        local FOVLabel = Instance.new("TextLabel")
        FOVLabel.Size = UDim2.new(0, 160, 1, 0)
        FOVLabel.Position = UDim2.new(0, 10, 0, 0)
        FOVLabel.BackgroundTransparency = 1
        FOVLabel.Text = "Поле зрения (FOV)"
        FOVLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
        FOVLabel.Font = Enum.Font.GothamMedium
        FOVLabel.TextSize = 12
        FOVLabel.TextXAlignment = Enum.TextXAlignment.Left
        FOVLabel.Parent = FOVFrame

        local FOVBtn = Instance.new("TextButton")
        FOVBtn.Size = UDim2.new(0, 75, 0, 20)
        FOVBtn.Position = UDim2.new(1, -83, 0.5, -10)
        FOVBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        FOVBtn.Text = "70 (Default)"
        FOVBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        FOVBtn.Font = Enum.Font.GothamBold
        FOVBtn.TextSize = 11
        FOVBtn.Parent = FOVFrame

        local FBCorn = Instance.new("UICorner")
        FBCorn.CornerRadius = UDim.new(0, 4)
        FBCorn.Parent = FOVBtn

        FOVBtn.MouseButton1Click:Connect(function()
            if Config.FOVValue == 70 then
                Config.FOVValue = 95
                FOVBtn.Text = "95 FOV"
            elseif Config.FOVValue == 95 then
                Config.FOVValue = 110
                FOVBtn.Text = "110 FOV"
            else
                Config.FOVValue = 70
                FOVBtn.Text = "70 (Default)"
            end
        end)
    end

    CreateToggle(ExploitsTab, "Perfect Skill Check", "PerfectSkillCheck")

    do
        local TpFrame = Instance.new("Frame")
        TpFrame.Size = UDim2.new(1, -5, 0, 35)
        TpFrame.BackgroundColor3 = Color3.fromRGB(22, 22, 30)
        TpFrame.BorderSizePixel = 0
        TpFrame.Parent = ExploitsTab

        local TpCorn = Instance.new("UICorner")
        TpCorn.CornerRadius = UDim.new(0, 4)
        TpCorn.Parent = TpFrame

        local TpBtn = Instance.new("TextButton")
        TpBtn.Size = UDim2.new(1, -10, 0, 24)
        TpBtn.Position = UDim2.new(0, 5, 0.5, -12)
        TpBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
        TpBtn.Text = "ТП к ближайшему генератору"
        TpBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
        TpBtn.Font = Enum.Font.GothamMedium
        TpBtn.TextSize = 12
        TpBtn.Parent = TpFrame

        local TpBtnCorn = Instance.new("UICorner")
        TpBtnCorn.CornerRadius = UDim.new(0, 4)
        TpBtnCorn.Parent = TpBtn

        TpBtn.MouseButton1Click:Connect(function()
            local character = LocalPlayer.Character
            if not character then return end
            local hrp = character:FindFirstChild("HumanoidRootPart")
            if not hrp then return end

            local closest, closestDist = nil, math.huge
            for _, obj in ipairs(Workspace:GetDescendants()) do
                local n = obj.Name:lower()
                if (n:find("generator") or n:find("gen")) and obj:IsA("BasePart") then
                    local dist = (obj.Position - hrp.Position).Magnitude
                    if dist < closestDist then
                        closestDist = dist
                        closest = obj
                    end
                end
            end
            if closest then
                hrp.CFrame = CFrame.new(closest.Position + Vector3.new(0, 3, 0))
            end
        end)
    end

    UserInputService.InputBegan:Connect(function(input, gp)
        if gp then return end
        if input.KeyCode == Enum.KeyCode.RightControl or input.KeyCode == Enum.KeyCode.Insert then
            MainFrame.Visible = not MainFrame.Visible
        end
    end)

end)

if not success then
    warn("[Luna V4] Ошибка запуска: " .. tostring(launchError))
end
