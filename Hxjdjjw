do 
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local UserInputService = game:GetService("UserInputService")
    local RunService = game:GetService("RunService")
    local TweenService = game:GetService("TweenService")
    local Workspace = game:GetService("Workspace")

    local _G = {
        EspEnabled = true,
        InventoryCheck = true,
        LogCheck = true,
        GunEsp = true,
        KillAura = false,
        KillAuraRadius = 75,
        WalkSpeedValue = 16,
        JumpPowerValue = 50,
        SpeedEnabled = false,
        JumpEnabled = false,
        NoclipEnabled = false,
        SilentAim = false,
        SilentAimSmoothness = 0.1,
        SilentAimFOV = 150,
        SilentAimHeadshot = true,
        AutoCoin = false,
        AutoFarm = false,
        AutoGrabGun = false,
        GunDropEsp = true
    }

    local playerRoles = {}

    local COLORS = {
        Sheriff = Color3.fromRGB(0, 130, 255),
        Murderer = Color3.fromRGB(255, 35, 35),
        Innocent = Color3.fromRGB(45, 255, 85),
        DroppedGun = Color3.fromRGB(255, 0, 127) 
    }

    local WEAPONS = {
        Knife = "Murderer", Blade = "Murderer", Sword = "Murderer",
        Gun = "Sheriff", Revolver = "Sheriff", Pistol = "Sheriff"
    }

    -- ПАЛИТРА MIDNIGHT PHONK + GLASS
    local UI_COLORS = {
        Background = Color3.fromRGB(15, 5, 25),
        NeonPink = Color3.fromRGB(255, 0, 127),
        DarkPurple = Color3.fromRGB(25, 10, 40),
        LightPurple = Color3.fromRGB(40, 20, 60),
        TextWhite = Color3.fromRGB(255, 255, 255),
        TextMuted = Color3.fromRGB(180, 150, 200),
        GlassTransp = 0.35 
    }

    local sg = Instance.new("ScreenGui")
    sg.Name = "MM2_Midnight_Glass"
    sg.Parent = (gethui and gethui()) or game:GetService("CoreGui")
    sg.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    -- Главное окно
    local MainFrame = Instance.new("Frame")
    MainFrame.Size = UDim2.new(0, 680, 0, 580)
    MainFrame.Position = UDim2.new(0.5, -340, 0.5, -290)
    MainFrame.BackgroundColor3 = UI_COLORS.Background
    MainFrame.BackgroundTransparency = UI_COLORS.GlassTransp
    MainFrame.BorderSizePixel = 0
    MainFrame.Active = true
    MainFrame.Draggable = true
    MainFrame.Parent = sg

    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 14)
    MainCorner.Parent = MainFrame
    
    -- Стеклянная обводка
    local MainStroke = Instance.new("UIStroke")
    MainStroke.Color = UI_COLORS.NeonPink
    MainStroke.Thickness = 1
    MainStroke.Transparency = 0.5
    MainStroke.Parent = MainFrame

    -- Неоновое свечение на фоне
    local GlowFrame = Instance.new("Frame")
    GlowFrame.Size = UDim2.new(1, 10, 1, 10)
    GlowFrame.Position = UDim2.new(0, -5, 0, -5)
    GlowFrame.BackgroundColor3 = UI_COLORS.NeonPink
    GlowFrame.BackgroundTransparency = 0.85
    GlowFrame.BorderSizePixel = 0
    GlowFrame.ZIndex = -1
    GlowFrame.Parent = MainFrame

    local GlowCorner = Instance.new("UICorner")
    GlowCorner.CornerRadius = UDim.new(0, 18)
    GlowCorner.Parent = GlowFrame

    local ContentContainer = Instance.new("Frame")
    ContentContainer.Size = UDim2.new(1, 0, 1, 0)
    ContentContainer.BackgroundTransparency = 1
    ContentContainer.Parent = MainFrame

    -- Верхняя панель
    local TopGradient = Instance.new("Frame")
    TopGradient.Size = UDim2.new(1, 0, 0, 60)
    TopGradient.Position = UDim2.new(0, 0, 0, 0)
    TopGradient.BackgroundColor3 = UI_COLORS.DarkPurple
    TopGradient.BackgroundTransparency = 0.2
    TopGradient.BorderSizePixel = 0
    TopGradient.Parent = ContentContainer

    local TopGradientCorner = Instance.new("UICorner")
    TopGradientCorner.CornerRadius = UDim.new(0, 14)
    TopGradientCorner.Parent = TopGradient
    
    -- Выравниваем нижние углы верхней панели
    local BottomSquare = Instance.new("Frame")
    BottomSquare.Size = UDim2.new(1, 0, 0, 10)
    BottomSquare.Position = UDim2.new(0, 0, 1, -10)
    BottomSquare.BackgroundColor3 = UI_COLORS.DarkPurple
    BottomSquare.BackgroundTransparency = 0.2
    BottomSquare.BorderSizePixel = 0
    BottomSquare.Parent = TopGradient

    local TitleIcon = Instance.new("TextLabel")
    TitleIcon.Size = UDim2.new(0, 45, 1, 0)
    TitleIcon.Position = UDim2.new(0, 15, 0, 0)
    TitleIcon.BackgroundTransparency = 1
    TitleIcon.Text = "🦇"
    TitleIcon.TextColor3 = UI_COLORS.NeonPink
    TitleIcon.TextSize = 28
    TitleIcon.Font = Enum.Font.GothamBold
    TitleIcon.TextXAlignment = Enum.TextXAlignment.Center
    TitleIcon.Parent = TopGradient

    local Title = Instance.new("TextLabel")
    Title.Size = UDim2.new(0, 350, 0, 30)
    Title.Position = UDim2.new(0, 65, 0, 10)
    Title.BackgroundTransparency = 1
    Title.Text = "NIGHTFALL V4"
    Title.TextColor3 = UI_COLORS.TextWhite
    Title.TextSize = 24
    Title.Font = Enum.Font.GothamBold
    Title.TextXAlignment = Enum.TextXAlignment.Left
    Title.Parent = TopGradient

    local Subtitle = Instance.new("TextLabel")
    Subtitle.Size = UDim2.new(0, 250, 0, 18)
    Subtitle.Position = UDim2.new(0, 65, 0, 38)
    Subtitle.BackgroundTransparency = 1
    Subtitle.Text = "Midnight Phonk Edition"
    Subtitle.TextColor3 = UI_COLORS.NeonPink
    Subtitle.TextSize = 12
    Subtitle.Font = Enum.Font.Gotham
    Subtitle.TextXAlignment = Enum.TextXAlignment.Left
    Subtitle.Parent = TopGradient

    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Size = UDim2.new(0, 36, 0, 36)
    CloseBtn.Position = UDim2.new(1, -45, 0, 12)
    CloseBtn.BackgroundColor3 = UI_COLORS.LightPurple
    CloseBtn.BackgroundTransparency = 0.5
    CloseBtn.Text = "✕"
    CloseBtn.TextColor3 = UI_COLORS.TextMuted
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.TextSize = 18
    CloseBtn.BorderSizePixel = 0
    CloseBtn.Parent = TopGradient

    local CloseCorner = Instance.new("UICorner")
    CloseCorner.CornerRadius = UDim.new(0, 8)
    CloseCorner.Parent = CloseBtn

    local HomeBarContainer = Instance.new("Frame")
    HomeBarContainer.Size = UDim2.new(0, 140, 0, 12)
    HomeBarContainer.Position = UDim2.new(0.5, -70, 1, -20)
    HomeBarContainer.BackgroundTransparency = 1
    HomeBarContainer.Active = true
    HomeBarContainer.Visible = false
    HomeBarContainer.Parent = sg

    local HomeBar = Instance.new("TextButton")
    HomeBar.Size = UDim2.new(1, 0, 0, 5)
    HomeBar.Position = UDim2.new(0, 0, 0.5, -2)
    HomeBar.BackgroundColor3 = UI_COLORS.NeonPink
    HomeBar.BackgroundTransparency = 0.2
    HomeBar.Text = ""
    HomeBar.BorderSizePixel = 0
    HomeBar.Parent = HomeBarContainer

    local HomeBarCorner = Instance.new("UICorner")
    HomeBarCorner.CornerRadius = UDim.new(1, 0)
    HomeBarCorner.Parent = HomeBar

    CloseBtn.MouseEnter:Connect(function()
        TweenService:Create(CloseBtn, TweenInfo.new(0.15), {BackgroundColor3 = UI_COLORS.NeonPink, TextColor3 = UI_COLORS.TextWhite}):Play()
    end)
    CloseBtn.MouseLeave:Connect(function()
        TweenService:Create(CloseBtn, TweenInfo.new(0.15), {BackgroundColor3 = UI_COLORS.LightPurple, TextColor3 = UI_COLORS.TextMuted}):Play()
    end)
    CloseBtn.MouseButton1Click:Connect(function()
        MainFrame.Visible = false
        HomeBarContainer.Visible = true
    end)

    HomeBar.MouseButton1Click:Connect(function()
        HomeBarContainer.Visible = false
        MainFrame.Visible = true
    end)

    -- Боковая панель
    local Sidebar = Instance.new("Frame")
    Sidebar.Size = UDim2.new(0, 160, 1, -60)
    Sidebar.Position = UDim2.new(0, 0, 0, 60)
    Sidebar.BackgroundColor3 = UI_COLORS.DarkPurple
    Sidebar.BackgroundTransparency = 0.4
    Sidebar.BorderSizePixel = 0
    Sidebar.Parent = ContentContainer

    local Container = Instance.new("Frame")
    Container.Size = UDim2.new(1, -175, 1, -75)
    Container.Position = UDim2.new(0, 170, 0, 70)
    Container.BackgroundTransparency = 1
    Container.Parent = ContentContainer

    local EspPage = Instance.new("ScrollingFrame")
    local CombatPage = Instance.new("ScrollingFrame")
    local MovePage = Instance.new("ScrollingFrame")
    local TeleportPage = Instance.new("ScrollingFrame")
    local TrollingPage = Instance.new("ScrollingFrame")
    local activePage = EspPage

    for _, p in ipairs({EspPage, CombatPage, MovePage, TeleportPage, TrollingPage}) do
        p.Size = UDim2.new(1, 0, 1, 0)
        p.BackgroundTransparency = 1
        p.BorderSizePixel = 0
        p.ScrollBarThickness = 2
        p.ScrollBarImageColor3 = UI_COLORS.NeonPink
        p.ScrollBarImageTransparency = 0.2
        p.Visible = false
        p.Parent = Container
        local list = Instance.new("UIListLayout")
        list.Padding = UDim.new(0, 12)
        list.HorizontalAlignment = Enum.HorizontalAlignment.Center
        list.Parent = p
    end
    EspPage.Visible = true

    local function switchTab(targetPage)
        if (activePage == targetPage) then return end
        targetPage.CanvasPosition = Vector2.new(0, 0)
        targetPage.Visible = true
        TweenService:Create(activePage, TweenInfo.new(0.15), {BackgroundTransparency = 1}):Play()
        activePage.Visible = false
        TweenService:Create(targetPage, TweenInfo.new(0.2, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Size = UDim2.new(1, 0, 1, 0)}):Play()
        activePage = targetPage
    end

    local currentActiveButton = nil
    local function createTabBtn(text, icon, pos, targetPage)
        local b = Instance.new("TextButton")
        b.Size = UDim2.new(1, -15, 0, 48)
        b.Position = pos
        b.BackgroundColor3 = UI_COLORS.LightPurple
        b.BackgroundTransparency = 0.8
        b.BorderSizePixel = 0
        b.Text = " " .. icon .. " " .. text
        b.TextColor3 = UI_COLORS.TextMuted
        b.Font = Enum.Font.GothamBold
        b.TextSize = 13
        b.TextXAlignment = Enum.TextXAlignment.Left
        b.Parent = Sidebar

        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 10)
        btnCorner.Parent = b

        if (targetPage == EspPage) then
            b.BackgroundTransparency = 0.3
            b.TextColor3 = UI_COLORS.NeonPink
            currentActiveButton = b
            local activeLine = Instance.new("Frame")
            activeLine.Size = UDim2.new(0, 3, 1, -12)
            activeLine.Position = UDim2.new(1, -3, 0, 6)
            activeLine.BackgroundColor3 = UI_COLORS.NeonPink
            activeLine.BorderSizePixel = 0
            activeLine.Parent = b
        end

        b.MouseButton1Click:Connect(function()
            if currentActiveButton then
                for _, child in ipairs(currentActiveButton:GetChildren()) do
                    if child:IsA("Frame") then child:Destroy() end
                end
                TweenService:Create(currentActiveButton, TweenInfo.new(0.15), {BackgroundTransparency = 0.8, TextColor3 = UI_COLORS.TextMuted}):Play()
            end
            currentActiveButton = b
            TweenService:Create(b, TweenInfo.new(0.15), {BackgroundTransparency = 0.3, TextColor3 = UI_COLORS.NeonPink}):Play()
            local activeLine = Instance.new("Frame")
            activeLine.Size = UDim2.new(0, 3, 1, -12)
            activeLine.Position = UDim2.new(1, -3, 0, 6)
            activeLine.BackgroundColor3 = UI_COLORS.NeonPink
            activeLine.BorderSizePixel = 0
            activeLine.Parent = b
            switchTab(targetPage)
        end)
    end

    createTabBtn("Визуал", "👁️", UDim2.new(0, 8, 0, 15), EspPage)
    createTabBtn("Бой", "⚔️", UDim2.new(0, 8, 0, 68), CombatPage)
    createTabBtn("Движение", "🏃", UDim2.new(0, 8, 0, 121), MovePage)
    createTabBtn("Телепорт", "🌀", UDim2.new(0, 8, 0, 174), TeleportPage)
    createTabBtn("Троллинг", "💀", UDim2.new(0, 8, 0, 227), TrollingPage)

    -- UI Элементы управления
    local function createToggle(parent, text, startState, callback)
        local state = startState
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, -10, 0, 52)
        row.BackgroundColor3 = UI_COLORS.DarkPurple
        row.BackgroundTransparency = 0.5
        row.BorderSizePixel = 0
        row.Parent = parent

        local rowCorner = Instance.new("UICorner")
        rowCorner.CornerRadius = UDim.new(0, 10)
        rowCorner.Parent = row

        local rowStroke = Instance.new("UIStroke")
        rowStroke.Color = UI_COLORS.LightPurple
        rowStroke.Thickness = 1
        rowStroke.Parent = row

        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.7, 0, 1, 0)
        lbl.Position = UDim2.new(0, 15, 0, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextColor3 = UI_COLORS.TextWhite
        lbl.Font = Enum.Font.GothamMedium
        lbl.TextSize = 13
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = row

        local switch = Instance.new("TextButton")
        switch.Size = UDim2.new(0, 48, 0, 26)
        switch.Position = UDim2.new(1, -58, 0, 13)
        switch.BackgroundColor3 = (state and UI_COLORS.NeonPink) or UI_COLORS.LightPurple
        switch.Text = ""
        switch.BorderSizePixel = 0
        switch.Parent = row

        local swCorner = Instance.new("UICorner")
        swCorner.CornerRadius = UDim.new(1, 0)
        swCorner.Parent = switch

        local dot = Instance.new("Frame")
        dot.Size = UDim2.new(0, 20, 0, 20)
        dot.Position = (state and UDim2.new(1, -24, 0, 3)) or UDim2.new(0, 3, 0, 3)
        dot.BackgroundColor3 = UI_COLORS.TextWhite
        dot.BorderSizePixel = 0
        dot.Parent = switch

        local dotCorner = Instance.new("UICorner")
        dotCorner.CornerRadius = UDim.new(1, 0)
        dotCorner.Parent = dot

        switch.MouseButton1Click:Connect(function()
            state = not state
            local targetColor = (state and UI_COLORS.NeonPink) or UI_COLORS.LightPurple
            local targetPos = (state and UDim2.new(1, -24, 0, 3)) or UDim2.new(0, 3, 0, 3)
            
            if state then
                local glow = Instance.new("UIStroke")
                glow.Color = UI_COLORS.NeonPink
                glow.Thickness = 2
                glow.Transparency = 0.5
                glow.Parent = row
                TweenService:Create(glow, TweenInfo.new(0.5), {Transparency = 1}):Play()
                game.Debris:AddItem(glow, 0.5)
            end

            TweenService:Create(switch, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {BackgroundColor3 = targetColor}):Play()
            TweenService:Create(dot, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {Position = targetPos}):Play()
            callback(state)
        end)
    end

    local function createSlider(parent, text, minVal, maxVal, defaultVal, callback)
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, -10, 0, 75)
        row.BackgroundColor3 = UI_COLORS.DarkPurple
        row.BackgroundTransparency = 0.5
        row.BorderSizePixel = 0
        row.Parent = parent

        local rowCorner = Instance.new("UICorner")
        rowCorner.CornerRadius = UDim.new(0, 10)
        rowCorner.Parent = row

        local rowStroke = Instance.new("UIStroke")
        rowStroke.Color = UI_COLORS.LightPurple
        rowStroke.Thickness = 1
        rowStroke.Parent = row

        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(1, -20, 0, 28)
        lbl.Position = UDim2.new(0, 15, 0, 8)
        lbl.BackgroundTransparency = 1
        lbl.Text = text .. " : " .. defaultVal
        lbl.TextColor3 = UI_COLORS.TextWhite
        lbl.Font = Enum.Font.GothamMedium
        lbl.TextSize = 13
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = row

        local sliderBg = Instance.new("Frame")
        sliderBg.Size = UDim2.new(1, -30, 0, 6)
        sliderBg.Position = UDim2.new(0, 15, 0, 48)
        sliderBg.BackgroundColor3 = UI_COLORS.LightPurple
        sliderBg.BorderSizePixel = 0
        sliderBg.Parent = row

        local sliderBgCorner = Instance.new("UICorner")
        sliderBgCorner.CornerRadius = UDim.new(1, 0)
        sliderBgCorner.Parent = sliderBg

        local sliderFill = Instance.new("Frame")
        sliderFill.Size = UDim2.new((defaultVal - minVal) / (maxVal - minVal), 0, 1, 0)
        sliderFill.BackgroundColor3 = UI_COLORS.NeonPink
        sliderFill.BorderSizePixel = 0
        sliderFill.Parent = sliderBg

        local sliderFillCorner = Instance.new("UICorner")
        sliderFillCorner.CornerRadius = UDim.new(1, 0)
        sliderFillCorner.Parent = sliderFill

        local sliderBtn = Instance.new("TextButton")
        sliderBtn.Size = UDim2.new(0, 18, 0, 18)
        sliderBtn.Position = UDim2.new((defaultVal - minVal) / (maxVal - minVal), -9, 0, -6)
        sliderBtn.BackgroundColor3 = UI_COLORS.TextWhite
        sliderBtn.Text = ""
        sliderBtn.BorderSizePixel = 0
        sliderBtn.Parent = row

        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(1, 0)
        btnCorner.Parent = sliderBtn

        local dragging = false
        sliderBtn.MouseButton1Down:Connect(function()
            dragging = true
            while dragging and row.Parent do
                local mousePos = UserInputService:GetMouseLocation()
                local rowPos = sliderBg.AbsolutePosition.X
                local rowWidth = sliderBg.AbsoluteSize.X
                local percent = math.clamp((mousePos.X - rowPos) / rowWidth, 0, 1)
                local value = minVal + (percent * (maxVal - minVal))
                
                TweenService:Create(sliderFill, TweenInfo.new(0.05), {Size = UDim2.new(percent, 0, 1, 0)}):Play()
                TweenService:Create(sliderBtn, TweenInfo.new(0.05), {Position = UDim2.new(percent, -9, 0, -6)}):Play()
                
                lbl.Text = text .. " : " .. math.floor(value)
                callback(math.floor(value))
                task.wait()
            end
        end)

        UserInputService.InputEnded:Connect(function(input)
            if (input.UserInputType == Enum.UserInputType.MouseButton1) then
                dragging = false
            end
        end)
    end

    local function createActionButton(parent, text, callback)
        local row = Instance.new("Frame")
        row.Size = UDim2.new(1, -10, 0, 52)
        row.BackgroundColor3 = UI_COLORS.DarkPurple
        row.BackgroundTransparency = 0.5
        row.BorderSizePixel = 0
        row.Parent = parent

        local rowCorner = Instance.new("UICorner")
        rowCorner.CornerRadius = UDim.new(0, 10)
        rowCorner.Parent = row

        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, -20, 0, 36)
        btn.Position = UDim2.new(0, 10, 0, 8)
        btn.BackgroundColor3 = UI_COLORS.LightPurple
        btn.Text = text
        btn.TextColor3 = UI_COLORS.TextWhite
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 13
        btn.BorderSizePixel = 0
        btn.Parent = row

        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 8)
        btnCorner.Parent = btn
        
        local btnStroke = Instance.new("UIStroke")
        btnStroke.Color = UI_COLORS.NeonPink
        btnStroke.Thickness = 1
        btnStroke.Transparency = 0.5
        btnStroke.Parent = btn

        btn.MouseEnter:Connect(function()
            TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = UI_COLORS.NeonPink, TextColor3 = Color3.fromRGB(0,0,0)}):Play()
        end)
        btn.MouseLeave:Connect(function()
            TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = UI_COLORS.LightPurple, TextColor3 = UI_COLORS.TextWhite}):Play()
        end)
        btn.MouseButton1Click:Connect(callback)
    end

    -- Наполнение меню
    createToggle(EspPage, "Master Role ESP", _G.EspEnabled, function(v) _G.EspEnabled = v end)
    createToggle(EspPage, "Gun Drop ESP (Неон Розовый)", _G.GunDropEsp, function(v) _G.GunDropEsp = v end)

    createToggle(CombatPage, "Murderer Kill Aura", _G.KillAura, function(v) _G.KillAura = v end)
    createSlider(CombatPage, "Радиус ауры убийства", 10, 150, 75, function(v) _G.KillAuraRadius = v end)
    createToggle(CombatPage, "Sheriff Silent Aim", _G.SilentAim, function(v) _G.SilentAim = v end)
    createSlider(CombatPage, "Silent Aim FOV", 50, 500, 150, function(v) _G.SilentAimFOV = v end)

    createToggle(MovePage, "Взлом скорости", _G.SpeedEnabled, function(v) _G.SpeedEnabled = v end)
    createSlider(MovePage, "Скорость бега", 16, 100, 16, function(v) _G.WalkSpeedValue = v end)
    createToggle(MovePage, "Noclip (Проход сквозь стены)", _G.NoclipEnabled, function(v) _G.NoclipEnabled = v end)
    
    createToggle(MovePage, "Auto-Collect Coins (Обновленный код)", _G.AutoCoin, function(v) _G.AutoCoin = v end)
    createToggle(MovePage, "Auto-Win / AFK Farm (Невидимая база)", _G.AutoFarm, function(v) _G.AutoFarm = v end)
    createToggle(MovePage, "Auto-Grab Gun", _G.AutoGrabGun, function(v) _G.AutoGrabGun = v end)

    createActionButton(TeleportPage, "💀 ТП к Маньяку", function()
        for _, player in ipairs(Players:GetPlayers()) do
            if ((playerRoles[player.Name] == "Murderer") and player.Character and player.Character:FindFirstChild("HumanoidRootPart")) then
                local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if hrp then hrp.CFrame = player.Character.HumanoidRootPart.CFrame * CFrame.new(0, 2, 0) end
                break
            end
        end
    end)

    createActionButton(TeleportPage, "🔫 ТП к Шерифу", function()
        for _, player in ipairs(Players:GetPlayers()) do
            if ((playerRoles[player.Name] == "Sheriff") and player.Character and player.Character:FindFirstChild("HumanoidRootPart")) then
                local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                if hrp then hrp.CFrame = player.Character.HumanoidRootPart.CFrame * CFrame.new(0, 2, 0) end
                break
            end
        end
    end)

    -- ЛОГИКА СКРИПТА
    
    -- Noclip
    RunService.Stepped:Connect(function()
        if (_G.NoclipEnabled and LocalPlayer.Character) then
            for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end)

    -- Speed
    RunService.Stepped:Connect(function()
        if (_G.SpeedEnabled and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) then
            local hrp = LocalPlayer.Character.HumanoidRootPart
            local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if (hum and (hum.MoveDirection.Magnitude > 0)) then
                hrp.AssemblyLinearVelocity = hum.MoveDirection * _G.WalkSpeedValue
            end
        end
    end)

    -- Silent Aim
    local function getClosestPlayerInFOV()
        if (not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) then return nil end
        local camera = Workspace.CurrentCamera
        local closestPlayer = nil
        local closestDistance = math.huge

        for _, player in ipairs(Players:GetPlayers()) do
            if ((player ~= LocalPlayer) and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid")) then
                if playerRoles[player.Name] == "Murderer" then
                    local targetHrp = player.Character.HumanoidRootPart
                    local targetHum = player.Character.Humanoid
                    if (targetHum.Health > 0) then
                        local screenPos, onScreen = camera:WorldToScreenPoint(targetHrp.Position)
                        if onScreen then
                            local mousePos = UserInputService:GetMouseLocation()
                            local distance = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                            if ((distance <= _G.SilentAimFOV) and (distance < closestDistance)) then
                                closestDistance = distance
                                closestPlayer = player
                            end
                        end
                    end
                end
            end
        end
        return closestPlayer
    end

    local oldMouse = nil
    local toolConnection = nil
    local function setupSilentAim()
        if toolConnection then toolConnection:Disconnect() end
        toolConnection = LocalPlayer.CharacterAdded:Connect(function(character)
            task.wait(0.5)
            local tool = character:FindFirstChildOfClass("Tool")
            if tool then
                local gunCheck = tool.Name:lower():find("gun") or tool.Name:lower():find("revolver") or tool.Name:lower():find("pistol") or (tool.Name == "SheriffGun")
                if gunCheck then
                    local mouse = LocalPlayer:GetMouse()
                    if not oldMouse then
                        oldMouse = mouse.Button1Down
                        mouse.Button1Down = function()
                            if (_G.SilentAim and (playerRoles[LocalPlayer.Name] == "Sheriff")) then
                                local target = getClosestPlayerInFOV()
                                if target then
                                    local head = target.Character:FindFirstChild("Head")
                                    if head then
                                        local camera = Workspace.CurrentCamera
                                        local oldCFrame = camera.CFrame
                                        camera.CFrame = CFrame.lookAt(camera.CFrame.Position, head.Position)
                                        oldMouse()
                                        task.wait(0.02)
                                        camera.CFrame = oldCFrame
                                        return
                                    end
                                end
                            end
                            oldMouse()
                        end
                    end
                end
            end
        end)
    end
    setupSilentAim()
    LocalPlayer.CharacterAdded:Connect(setupSilentAim)

    -- Auto-Farm & Coins (с интеграцией нового кода)
    task.spawn(function()
        while true do
            local character = LocalPlayer.Character
            local hrp = character and character:FindFirstChild("HumanoidRootPart")
            local hum = character and character:FindFirstChildOfClass("Humanoid")
            
            if hrp and hum and hum.Health > 0 then
                local gunDrop = Workspace:FindFirstChild("GunDrop")
                if gunDrop and gunDrop:IsA("BasePart") then
                    if _G.GunDropEsp then
                        local gEsp = gunDrop:FindFirstChild("GunESP") or Instance.new("Highlight")
                        gEsp.Name = "GunESP"
                        gEsp.Parent = gunDrop
                        gEsp.FillColor = COLORS.DroppedGun
                        gEsp.OutlineColor = UI_COLORS.TextWhite
                        gEsp.Enabled = true
                    end
                    if _G.AutoGrabGun and playerRoles[LocalPlayer.Name] ~= "Murderer" then
                        if firetouchinterest then
                            firetouchinterest(hrp, gunDrop, 0)
                            task.wait()
                            firetouchinterest(hrp, gunDrop, 1)
                        else
                            hrp.CFrame = gunDrop.CFrame
                        end
                    end
                end

                -- Интеграция твоего кода для автофарма монет
                if _G.AutoCoin and not _G.AutoFarm then
                    for _, obj in pairs(Workspace:GetDescendants()) do
                        -- Мгновенно прерываем цикл, если фарм отключили в интерфейсе
                        if not _G.AutoCoin then break end 
                        
                        local name = obj.Name:lower()
                        if (name:find("coin") or name:find("money") or name:find("gold")) and obj:IsA("BasePart") then
                            
                            if firetouchinterest then
                                firetouchinterest(hrp, obj, 0)
                                task.wait()
                                firetouchinterest(hrp, obj, 1)
                            else
                                local savedPosition = hrp.CFrame
                                hrp.CFrame = obj.CFrame
                                task.wait(0.2)
                                hrp.CFrame = savedPosition
                            end
                        end
                    end
                end

                if _G.AutoFarm then
                    local farmPlatform = Workspace:FindFirstChild("XenoFarmPlatform")
                    if not farmPlatform then
                        farmPlatform = Instance.new("Part")
                        farmPlatform.Name = "XenoFarmPlatform"
                        farmPlatform.Size = Vector3.new(20, 1, 20)
                        farmPlatform.Position = Vector3.new(0, 500, 0)
                        farmPlatform.Anchored = true
                        farmPlatform.Transparency = 1
                        farmPlatform.Parent = Workspace
                    end
                    
                    if (hrp.Position - farmPlatform.Position).Magnitude > 15 then
                        hrp.CFrame = farmPlatform.CFrame * CFrame.new(0, 3, 0)
                    end
                    
                    hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                else
                    local farmPlatform = Workspace:FindFirstChild("XenoFarmPlatform")
                    if farmPlatform then farmPlatform:Destroy() end
                end
            end
            task.wait(0.2) -- Чуть уменьшил задержку для большей отзывчивости
        end
    end)

    -- ESP & Role Logic
    local function applyHighlight(player, role)
        local character = player.Character
        if (not character or not _G.EspEnabled) then
            if (character and character:FindFirstChild("RoleESP")) then character.RoleESP:Destroy() end
            return
        end
        local color = COLORS[role] or COLORS.Innocent
        local h = character:FindFirstChild("RoleESP") or Instance.new("Highlight")
        h.Name = "RoleESP"
        h.Parent = character
        h.FillColor = color
        h.FillTransparency = 0.5
        h.OutlineColor = color
        h.OutlineTransparency = 0
        h.Enabled = true
    end

    local function checkInventory(player)
        if ((player == LocalPlayer) or not _G.InventoryCheck) then return nil end
        local character = player.Character
        local backpack = player:FindFirstChild("Backpack")
        
        local function scan(container)
            if not container then return nil end
            for _, item in ipairs(container:GetChildren()) do
                if item:IsA("Tool") then
                    for keyword, role in pairs(WEAPONS) do
                        if item.Name:lower():find(keyword:lower()) then return role end
                    end
                end
            end
            return nil
        end
        
        return scan(character) or scan(backpack)
    end

    task.spawn(function()
        while true do
            for _, player in ipairs(Players:GetPlayers()) do
                if (player.Character and player.Character:FindFirstChild("HumanoidRootPart") and (player ~= LocalPlayer)) then
                    if not _G.EspEnabled then
                        if player.Character:FindFirstChild("RoleESP") then player.Character.RoleESP:Destroy() end
                    else
                        local det = checkInventory(player)
                        if det then
                            playerRoles[player.Name] = det
                            applyHighlight(player, det)
                        elseif playerRoles[player.Name] then
                            applyHighlight(player, playerRoles[player.Name])
                        else
                            applyHighlight(player, "Innocent")
                        end
                    end
                end
            end
            task.wait(0.5)
        end
    end)

    print("🦇 NIGHTFALL V4: MIDNIGHT GLASS EDITION LOADED")
end
